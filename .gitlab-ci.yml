pages:
    stage: deploy
    script:
        - mkdir .public
        - cp -r doxygen/html/. .public
        - mv .public public
    artifacts:
        paths:
            - public
    only:
        - master